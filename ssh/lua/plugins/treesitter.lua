return {
  {
    "nvim-treesitter/nvim-treesitter",
    -- opts = {
    --   ensure_installed = { "latex" },
    -- },
    -- opts = function(_, opts)
    --   opts.ensure_installed = {}
    -- end,
  },
}
